[project]
name = "freya-backend"
version = "0.1.0"
description = "Backend for Freya"
readme = "README.md"
authors = [
    { name = "M", email = "lmiguelvargasf@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "litestar[standard]>=2.15.2",
]

[project.scripts]
freya-backend = "freya_backend:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
